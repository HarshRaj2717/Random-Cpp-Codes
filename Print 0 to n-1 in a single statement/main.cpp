#include <bits/stdc++.h>

using namespace std;

int main()
{
    for (int j = 0, i = INT_MAX; j < i; (i == INT_MAX ? (cin >> i) && 1 : j++)) i == INT_MAX ? i = INT_MAX : (cout << j << ' ' && 1);
    return 0;
}
